var printToFinal = function(n) {
  let arr = []
  let final = Math.pow(10,n) - 1;
  for(let i=1;i<=final;i++) {
    arr.push(i)
  }
  return arr;

}

var printtoFinal = function(n) {
  let final = Math.pow(10,n)-1;
  let arr = [];
  for(let i=1;i<final;i++) {
    arr.push(i)
  }
  return arr
}